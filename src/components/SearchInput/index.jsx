import './styles.css';

export const SearchInput = ({searchValue, onChange}) => {
    return(
        <input
        className="search-inputsssss"s
        value={searchValue}
        onChange={onChange}
        type="search" />
    )
}